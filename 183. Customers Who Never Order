import pandas as pd

def find_customers(customers: pd.DataFrame, orders: pd.DataFrame) -> pd.DataFrame:
    customers=customers.rename({"id":"customerId","name":"Customers"},axis=1)
    new_df=pd.merge(customers,orders,on="customerId",how="left")
    return new_df[new_df["id"].isna()][["Customers"]]
    
